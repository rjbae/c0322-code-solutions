/* exported getFirstChar */
function getFirstChar(string) {
  return string[0];
}
// - Define a function named getFirstChar with one parameter of string.
// - Trying to find the first character of a string.
// - In order to do so, we must access the first index [0] of the string.
// - Return the value of string[0].
