/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// Trying to find the last character of a string.
// Define a function named getLastChar with one parameter of string.
// In order to find the last character we must access the last index of the string.
// To access the last index of the string, we have to subtract the length of the string by 1.
// Return the value of the string.
