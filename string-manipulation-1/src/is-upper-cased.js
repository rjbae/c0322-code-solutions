/* exported isUpperCased */
function isUpperCased(word) {
  if (word.toUpperCase() === word) {
    return true;
  } else {
    return false;
  }
}

// - Checking to see if the value of word is all uppercased.
// - Define a function named isUpperCased with one parameter of word.
// - Use a boolean to check if the value of word is all uppercased.
// - Create a conditional using an if statement.
// - In the condition of the if statement, check to see if the value of word is all uppercased
//  by using the toUpperCase() function and making it strictly equal to the value of word.
// - If the condition is true, return the boolean value of true.
// - If the condition is not true, return the boolean value of false.
