/* exported last */
function last(array) {
  return array[array.length - 1];
}

// - Looking for the last element of the array.
// - Define a function named last with one parameter array.
// - In order to find the last element we must use bracket notation and to find
//   the last element of an array we can subtract the length of the array by 1.
