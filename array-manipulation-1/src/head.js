/* exported head */
function head(array) {
  return array[0];
}

// - Define a function name head with one parameter of array.
// - Array indexes start 0 so that's where the first element would be found.
// - Return the first element in array by using bracket notation to indicate the index.
