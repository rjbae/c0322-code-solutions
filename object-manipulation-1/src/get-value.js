/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// - Obtain the value in object from the key property.
// - Define a function named getValue with two parameters object and key.
// - Return the value of the property by using bracket notation.
